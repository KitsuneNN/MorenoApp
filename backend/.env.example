# Desarrollo local con Docker Compose
DATABASE_URL=postgresql+psycopg://moreno:moreno_dev_password@postgres:5432/morenoapp
# Para ejecutar uvicorn directamente desde el host:
# DATABASE_URL=postgresql+psycopg://moreno:moreno_dev_password@localhost:5432/morenoapp

# JSON array. En producción se debe restringir a los dominios reales del frontend web.
CORS_ORIGINS=["http://localhost:8081","http://localhost:19006"]
