import { PhasePlaceholder } from '@/components/common/PhasePlaceholder';

export default function AdministracionScreen() {
  return (
    <PhasePlaceholder
      eyebrow="Catálogo"
      title="Administración"
      description="Desde esta sección se crearán y editarán productos, precios, stock mínimo, unidades y códigos de barras."
    />
  );
}
